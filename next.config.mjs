/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [{ hostname: "bldenfbpieqqgubbmnck.supabase.co", protocol: "https", port: "" }],
  },
};

export default nextConfig;
